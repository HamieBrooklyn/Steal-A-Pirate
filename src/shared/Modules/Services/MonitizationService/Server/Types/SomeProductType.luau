local SharedTypes = require(script.Parent.Parent.Parent.Types)

local function SomeProductType(player: Player, wasPurchased: boolean, data: SharedTypes.ProductInfo)
	if wasPurchased then
	end
end

return SomeProductType
