local SharedTypes = require(script.Parent.Parent.Parent.Types)

local function AdminCommands(player: Player, wasPurchased: boolean, data: SharedTypes.ProductInfo)
	if wasPurchased then
	end
end

return AdminCommands
